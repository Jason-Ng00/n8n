## Dev Logs - 2026-04-03
- **Update**: User reported absolute success! The Python node spawned flawlessly using the patched image execution layout.
- **Requirement**: Unlocked the Python standard library inside n8n sandbox and appended `yfinance`, `pandas`, and `mplfinance` to pipeline installs.
- **Implementation**: 
  - Overrode strict n8n Python sandboxing using native `N8N_RUNNERS_STDLIB_ALLOW="*"` and `N8N_RUNNERS_EXTERNAL_ALLOW="*"` ENV flags.
  - Added the required financial engineering libraries to `requirements.txt` to safely bake them into the isolated `/opt/venv` during container initialization.
